import { getStructuredPathInfo } from "../StateProperty/getStructuredPathInfo";
export function resolveListIndex(engine, path, indexes) {
    const info = getStructuredPathInfo(path);
    return null;
}
